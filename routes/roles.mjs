// import express from 'express'
// const router = express.Router()

// import rolesControler from '../controller/roles.mjs'
// import rolesvalidate from '../middleware/roles.mjs'

// const { createRole, updateRole, deleteRole, getRoles } = rolesControler
// const { validate, validateId, queryValidation } = rolesvalidate

// router.post('/', validate, createRole)
// router.put('/', validate, validateId, updateRole)
// router.delete('/', validateId, deleteRole)
// router.get('/', queryValidation, getRoles)

// export default router
